{
  "name": "turnos",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "tsc -p jsconfig.json"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
