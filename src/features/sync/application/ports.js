/**
 * SyncPort contract for synchronization with remote storage.
 *
 * @typedef {Object} SyncPort
 * @property {() => Promise<void>} pull
 * @property {() => Promise<void>} push
 * @property {() => void} onLocalChange
 */

export {};
